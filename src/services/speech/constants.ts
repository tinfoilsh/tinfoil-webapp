export const SPEECH = {
  MODEL: 'qwen3-tts',
  VOICE: 'aiden',
  // Qwen's raw HTTP audio stream is mono, signed 16-bit little-endian PCM.
  SAMPLE_RATE: 24000,
  CHANNELS: 1,
  BYTES_PER_SAMPLE: 2,
  PCM_SCALE: 32768,
  BLOCK_SAMPLES: 6000,
  TARGET_CHUNK_CHARACTERS: 300,
  MAX_CHUNK_CHARACTERS: 600,
  MAX_TEXT_CHARACTERS: 100000,
  CONCURRENT_REQUESTS: 2,
  LOOKAHEAD_CHUNKS: 4,
  START_BUFFER_SECONDS: 12,
  HIGH_WATER_SECONDS: 60,
  MAX_CHUNK_AUDIO_SECONDS: 120,
  SCHEDULE_LEAD_SECONDS: 0.05,
  REQUEST_TIMEOUT_MS: 120000,
} as const
